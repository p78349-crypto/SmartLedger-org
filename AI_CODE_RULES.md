# AI 코드작업 규칙 (프로젝트 규칙)

우선순위 및 규칙 (사용자 지시 기준):

1) 필수(우선순위 1) — 코드 작성 금지: 한 줄은 절대 80자를 초과하지 않는다.
   - 모든 Dart/코드 파일의 새/수정된 모든 라인은 80자 이내여야 한다.
   - 자동 포맷 전후에도 라인 길이를 검사하여 위반 시 수정한다.

1.1) 필수(우선순위 1.1) — 변경 완료 후 백업/커밋: 작업 3건 이상 완료 시
      반드시 로컬 백업 또는 로컬 커밋 중 하나를 실행한다.
      - 여러 파일 변경을 포함한 연속 작업(3건 이상)을 마친 경우 적용.
      - 선택지: (a) 로컬 저장소에 커밋 수행, 또는 (b) 지정된 백업 스크립트를
         사용해 로컬 백업 생성. 둘 중 하나를 반드시 실행해야 함.
      - 이 규칙은 변경 손실을 방지하고 추적성을 유지하기 위한 필수 규칙이다.

1.2) 필수(우선순위 1.2) — 파일 크기 제한: Dart 파일은 300줄 이내를 유지한다.
      - **절대 기준: ≤ 300줄** (모든 Dart 파일에 적용)
      - 300줄 초과 파일: 반드시 분리 필요, **신규 코드 추가 금지**
      - 신규 파일 작성 시: 300줄 이내로 반드시 작성
      - 기존 파일 수정 시: 300줄 초과하면 먼저 분리 후 작업
      - 자동 생성 파일(*.g.dart, *.freezed.dart)은 이 규칙에서 제외한다.
      - 파일 분리 시 관련 테스트도 함께 분리/추가한다.

2) 필수(우선순위 2) — 불필요 작업 금지: 사용자가 명시적으로 지시하지 않은 변경을
   수행하지 않는다.
   - 자동으로 파일을 생성/삭제/수정하지 않는다. 사용자 지시가 있을 때만 수행.
   - 대규모 리팩터, 기능 추가, 또는 의도하지 않은 API 변경은 사전 승인 필요.

3) 권장사항(제안): 아래 항목은 기본 동작이 아닌 권장 규칙이다. 필요 시 사용자에게
   제안하고, 승인을 받은 경우에만 적용한다.
   - 변경은 작은 단위의 커밋(단일 목적, 최소 변경)으로 수행.
   - 변경 후 `dart format` 및 `dart analyze` 또는 해당 언어의 포맷/정적분석을 실행.
   - 민감 정보(키/암호 등)는 절대 저장소에 추가하지 않음.
   - 변경 전 간단한 요약(무엇을, 왜, 예상 영향)을 제공.
   - 다단계 작업은 TODO로 분해하고 단계별로 보고.

적용 절차(권장):
- 새 변경을 시작하기 전에 사용자에게 한 줄 요약(무엇을 할지)을 보냄.
- 변경을 수행할 때 `AI_CODE_RULES.md`의 1~2 우선순위를 반드시 지킴.
- 변경 완료 후 `dart analyze`(또는 해당 도구) 실행 결과를 보고.

예외: 보안상 긴급 대응(예: 보안 키 유출 차단 등) 시에는 즉시 수정할 수 있으나,
사후에 사용자에게 보고하고 변경을 설명한다.

문의: 이 규칙을 수정하거나 확장하려면 구체적인 항목을 알려주십시오.


 코드작업 체크리스트
우선순위 1 (필수)
• 	[ ] 모든 코드 라인은 80자 이내인지 확인했다
• 	[ ] 자동 포맷 후에도 라인 길이를 다시 검사했다
• 	[ ] 80자 초과 라인이 있으면 수정했다
우선순위 1.1 (필수)
• 	[ ] 3건 이상 작업을 완료했는가?
• 	[ ] 완료 후 로컬 커밋을 수행했는가?
• 	[ ] 또는 지정된 백업 스크립트로 백업을 생성했는가?
우선순위 1.2 (필수) — 파일 크기
• 	[ ] 모든 Dart 파일은 300줄 이내인지 확인했다
• 	[ ] 300줄 초과 파일에 신규 코드를 추가하지 않았다
• 	[ ] 300줄 초과 파일은 먼저 분리 후 작업했다
• 	[ ] 신규 파일은 300줄 이내로 작성했다
우선순위 2 (필수)
• 	[ ] 사용자 지시 없는 변경을 하지 않았다
• 	[ ] 자동 생성/삭제/수정 작업을 하지 않았다
• 	[ ] 대규모 리팩터·API 변경은 사전 승인 받았다
권장사항 (제안)
• 	[ ] 변경은 작은 단위 커밋으로 나눴다
• 	[ ] 변경 후  실행했다
• 	[ ] 변경 후  실행했다
• 	[ ] 민감 정보(키/암호 등)를 저장소에 추가하지 않았다
• 	[ ] 변경 전 간단한 요약을 작성했다
• 	[ ] 다단계 작업은 TODO로 분해하고 단계별로 보고했다

  지적재산권 무단사용 금지합니다
   info  이슈  경고 없이 깔끔하게 정리한다