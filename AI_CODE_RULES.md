# AI Agent 작업 규칙 (AI_CODE_RULES)

이 문서는 AI Agent 작업 규칙의 단일 기준(SSOT)입니다.
중복 문서는 이 파일로 통합되었습니다.

---

## 0. 최상위 원칙 (거짓 보고 금지)

AI Agent는 터미널에서 직접 실행한 결과만 보고합니다.
추측이나 간접 확인으로 "완료"를 선언하지 않습니다.

---

## 1. 작업 완료 전 필수 검증

AI Agent는 완료 보고 전 반드시 아래를 수행합니다.

### 1.1 코드 컴파일 및 정적 분석
- Flutter 프로젝트: `flutter analyze` 실행
- 에러/경고가 0건이어야 합니다.

### 1.2 테스트 실행
- `flutter test` 실행
- 모든 테스트가 통과해야 합니다.

### 1.3 보안 점검 (보안 관련 작업 시)
- `.\local_security_check.ps1` 실행
- `security_reports` 결과를 확인합니다.

---

## 2. 증거 기반 완료 보고

완료 보고에는 반드시 실행 결과를 포함합니다.

```markdown
### 🛡️ AI Agent 강제 검증 리포트
1. 정적 분석 (`flutter analyze`):
	- [ ] 실행 완료 (에러 0개)
2. 테스트 (`flutter test`):
	- [ ] 실행 완료 (전체 테스트 통과)
3. 보안 점검 (`local_security_check.ps1`):
	- [ ] 실행 완료 (취약점 없음) / [ ] 해당 없음

AI Agent 선언: 위 절차는 터미널에서 직접 실행했습니다.
```

---

## 3. 사용자 교차 검증 안내

완료 보고 시 사용자에게 교차 검증을 권장합니다.

예시:
"제가 터미널에서 모두 통과를 확인했지만,
실제 기기/에뮬레이터에서 한 번 더 확인해 주세요."

---

## 4. MIRA 정책 (필수 규칙)

### 4.1 라인 길이 제한
- 새/수정된 코드 라인은 80자 이내

### 4.2 작업 3건 이상 시 백업/커밋
- 3건 이상 작업 완료 시 백업 또는 로컬 커밋 수행

### 4.3 파일 크기 제한
- Dart 파일은 300줄 이내
- 300줄 초과 파일은 분리 후 작업

### 4.4 파일 관리 규칙
- `FILE_MANAGEMENT_RULES.md` 준수
- 250줄 이상: 분리 준비
- 280줄 이상: 신규 기능 추가 금지
- 분리 시 기존 import 호환이 필요하면 export로 연결
- 예시/데모 코드는 원칙적으로 `lib/`에 두지 않음

### 4.4.1 규칙 문서화
- 규칙 변경은 `FILE_MANAGEMENT_RULES.md` 절차 준수

### 4.5 UI 정책
- 메인 페이지는 순수 게이트웨이 역할
- 아이콘 크기: 48-60px (메인), 24px (하단)
- 기능은 더보기/햄버거로 그룹화
- 하단 기능 버튼은 한 화면에 3-4개로 제한

### 4.6 로직 분리 정책
- 신규 로직은 `lib/utils` 또는 `lib/logic`에 분리
- UI 파일에 비즈니스 로직 직접 작성 금지

---

## 5. 사용자 지시 없는 변경 금지

- 자동 생성/삭제/수정 금지
- 대규모 리팩터/기능 추가는 사전 승인 필요

---

## 6. 체크리스트

### 우선순위 1
- [ ] 모든 코드 라인 80자 이내
- [ ] 300줄 초과 파일에 신규 코드 없음

### 우선순위 1.1
- [ ] 3건 이상 작업 시 백업 또는 커밋 수행

### 우선순위 2
- [ ] 사용자 지시 없는 변경 없음
- [ ] 자동 생성/삭제/수정 없음

---

지적재산권 무단사용 금지
info 이슈 경고 없이 깔끔하게 정리한다
