# 가계부 앱팀 요청 체크리스트 (서버 연동)

작성일: 2026-02-23  
목적: 현재 서버(고정 동시 사용자 제한 없음) 기준으로 앱팀이 반드시 반영해야 할 작업 항목을 명확히 전달

## 0) 공통 인식
- 현재 서버 코드에는 동시 사용자 수 고정 제한이 없음
- 실제 운영 한계는 라이선스, 서버 자원, 오류율, 앱 호출 패턴으로 결정됨

## 1) 인증/라이선스 연동 (필수)
- [ ] 앱에서 `customerId`, `hwid`를 일관되게 관리
- [ ] 라이선스 검증 실패 사유별 UX 분기 처리
  - `not_found`
  - `inactive`
  - `hwid_mismatch`
  - `expired`
- [ ] 재로그인/기기변경 시 라이선스 재검증 루틴 반영
- [ ] 서버 주소/관리키 저장 및 편집 UI 제공
- [ ] `X-Admin-Key` 헤더를 모든 동기화 요청에 공통 적용
- [ ] 헬스 체크(`/api/ledger/health`)로 서버 상태 확인

## 2) 요청 안정성 (필수)
- [ ] API 타임아웃 설정(무한 대기 금지)
- [ ] 재시도 정책 적용(지수 백오프, 최대 횟수 제한)
- [ ] 중복 전송 방지 키(idempotency key) 적용
- [ ] 네트워크 불안정 시 사용자에게 상태 안내(재시도 중/실패)

## 3) 오프라인/동기화 처리
- [ ] 오프라인 저장 큐 구현(로컬 저장 후 온라인 시 재전송)
- [ ] 순서 보장(먼저 저장된 항목 먼저 전송)
- [ ] 중복 업로드 방지(동일 데이터 재전송 시 서버 중복 반영 방지)
- [ ] 앱 시작 시 Pull 실행(Delta 병합)
- [ ] 수동 동기화 버튼에서 Pull 재실행
- [ ] 테이블별 Push 라우팅 적용(`/push/{table}`)
- [ ] 삭제 동기화는 tombstone(`isDeleted`)로 처리
- [ ] 동기화 완료 후 `lastSyncAt` 갱신
- [ ] 충돌 처리 기준 명시(`updatedAt` 우선 등)

## 4) 성능/부하 제어
- [ ] 과도한 폴링 금지(고정 짧은 주기 호출 금지)
- [ ] 소량 배치 전송(다건을 묶어서 전송)
- [ ] 백그라운드 동기화 간격 조정(배터리/CPU 고려)

## 5) 보안 요구사항
- [ ] 토큰/관리키/민감정보 평문 저장 금지
- [ ] 앱 로그에서 민감정보 마스킹
- [ ] 분실/탈취 대응 시 revoke/wipe 상태 즉시 반영

## 6) 관측성/운영 추적
- [ ] 요청 단위 `requestId` 생성·전송
- [ ] 앱 로그에 API 응답시간(ms), 에러코드 기록
- [ ] 사용자 표시 메시지와 운영자 디버그 로그 분리
- [ ] 동기화 배치 ID 기록(성공/실패 수 집계)

## 7) 테스트 항목
- [ ] 정상 라이선스 검증 시나리오
- [ ] `hwid_mismatch` 시나리오
- [ ] 만료(`expired`) 시나리오
- [ ] 오프라인->온라인 재전송 시나리오
- [ ] 중복 전송 방지 시나리오
- [ ] 동시 사용자 증가(10→30→50) 단계 시나리오
- [ ] Pull/Push(테이블별) 동기화 시나리오
- [ ] 삭제 데이터(tombstone) 동기화 시나리오

## 8) 회신 요청 양식 (앱팀)
- 반영 완료 항목:
- 미완료 항목/사유:
- 위험요소:
- 예상 완료일:
- 릴리스 가능 여부: GO / HOLD / NO-GO
