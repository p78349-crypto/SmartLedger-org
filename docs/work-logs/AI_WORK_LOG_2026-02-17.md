# AI 작업 로그 (2026-02-17)

## 작업 개요
- 작업 일시: 2026-02-17
- 대상 파일: `lib/screens/income_split_screen.dart`, `lib/screens/income_split_screen.build.dart`, `lib/screens/income_split_screen.summary.dart`

## 작업 상세 내용
1. **쇼핑준비 기능 제거 및 장바구니 통합**
   - `AppRoutes.shoppingPrep` 라우트 삭제 및 관련 이동 로직 제거.
   - 메인 대시보드(Page 2)에서 '쇼핑준비' 아이콘 삭제.
   - `ShoppingCartScreen`의 `openPrepOnStart` 모드 파라미터 및 관련 분기 코드 전면 삭제.
   - 장바구니 화면 내에서 '체크박스'를 상시 노출하여 기능을 단일화.
   - `IngredientSearchListScreen`(식재료 검색)의 전송 버튼 명칭 변경: '쇼핑준비 보내기' -> '장바구니 보내기'.
2. **자산 입력 화면(상세/간편) UI 개선**
   - **타이틀 변경**:
     - `AssetInputScreen`: '계정명' -> '자산 상세 입력'
     - `AssetSimpleInputScreen`: '계정명' -> '자산 간편 입력'
   - **저장 버튼 상시 노출**:
     - 기존 리스트 하단의 저장 버튼을 삭제하고, 우측 하단에 **FloatingActionButton (FAB)** 적용.
     - 스크롤 중에도 언제든 즉시 저장이 가능하도록 개선.
     - FAB에 의한 가려짐 방지를 위해 하단 여백(80~100px) 확보.
   - **무검증 저장 기능 (Optional Fields)**:
     - 자산명, 금액 등 필수 입력 검증(Validator)을 제거하여 빈 칸인 상태로도 저장 허용.
     - 자산명 공백 시: '새 자산' (간편 입력은 '카테고리명')으로 자동 할당.
     - 금액 공백 시: 0원으로 자동 처리.
3. **지출/수입 입력 타이틀 정돈**
   - 상단 앱바 타이틀에서 불필요한 번호 접두사 제거.
   - '1-지출입력' -> '지출입력'
   - '2-지출입력(상세)' -> '지출입력(상세)'
4. **수입 배분 설정 UI 개선** (이전 세션 완료분)
   - 상단 앱바 타이틀 변경: '수입 배분 설정' -> '수입 배분'
   - 본문의 '배분 계획' 타이틀 가운데 정렬 적용.
   - 기존의 '지출 카테고리 편집', '수입 항목 편집' 버튼 및 섹션 타이틀 삭제.
   - 자산 이동 안내 캡션 삭제 (화면 간소화).
   - 메뉴 및 바텀 시트 타이틀명 변경:
     - '지출 카테고리 편집' / '카테고리별 예산 배분' -> '카테고리 배분'
     - '수입 항목 편집' / '수입 항목 배분' -> '수입을 자산으로'
5. **안내 메시지 정리** (이전 세션 완료분)
   - 화면 초기 안내 카드(Tip) 및 하단 설명 텍스트들을 모두 삭제.
   - 삭제된 내용을 우측 상단 메뉴의 **'사용법'** 팝업으로 통합 관리.
6. **요약 카드 최적화** (이전 세션 완료분)
   - 배분 현황 요약 카드를 화면 최상단으로 이동.
   - 카드 세로 크기를 약 40% 이상 축소하여 콤팩트한 디자인 적용 (글자 크기 및 간격 조정).
7. **저장 기능 접근성 개선** (이전 세션 완료분)
   - 우측 하단에 **'저장' 플로팅 버튼(FloatingActionButton)** 추가 (상시 노출).
   - 비활성 상태(`surfaceVariant`)의 deprecated 경고 해결 -> `surfaceContainerHighest`로 교체.
8. **메뉴 정리 및 최종 화면 압축** (이전 세션 완료분)
   - '배분 계획' 텍스트 제거 및 전체 입력 필드 간격 축소.
   - 모든 주요 기능이 스크롤 없이 한 화면에 들어오도록 최종 최적화.
   - 미사용 메뉴('수입배분 상세', '자산') 제거 및 관련 임포트(`app_routes.dart`) 정리.
9. **수입 입력 폼 개선** (이전 세션 완료분)
   - 수입 저장 시 매번 뜨던 '수입 분배' 안내창 제거 (즉시 저장 처리).
   - 수입 입력 시 메모란의 부적절한 안내 메시지('예: 마트 이름 + 간단 메모') 제거 (수입 모드일 때만 비표시).
10. **메인 화면 메뉴 정리** (이전 세션 완료분)
    - 페이지 2(수입 페이지)에서 '생활용품' 빠른 입력 아이콘 제거.
    - '날씨 입력' 아이콘을 수입 페이지(Page 2)에서 대시보드(Page 0)로 이동 배치.
11. **1억 프로젝트 화면 개선** (이전 세션 완료분)
    - 상단 설정(기어 아이콘)을 사각형 라운드 테두리 아이콘으로 변경하여 일관된 디자인 적용.
    - 앱바 높이에 영향을 주지 않도록 콤팩트한 사이즈 유지.
    - 설정 팝업(AlertDialog)의 타이틀을 '1억 프로젝트 설정'에서 **'1억 모으기'**로 변경.
    - 설정 팝업을 **전체 화면(Full-screen) 다이얼로그**로 확대하여 입력 편의성 개선.
    - 저장 시 토스트 메시지를 단순화하여 가독성 개선.
## 상태 확인
- `flutter analyze`: 이슈 없음 (No issues found!)
- 파일 크기 규칙: `income_split_screen.build.dart` (262줄) - 300줄 이내 준수.
- 라인 길이 규칙: 모든 신규/수정 라인 80자 이내 준수.

## 백업 정보
- 2026-02-17 작업 완료 후 `.\backup_project.ps1`을 통한 로컬 백업 완료.
