import 'package:flutter_test/flutter_test.dart';

// This file is documentation-only; the test ensures it stays compilable.
import 'package:smart_ledger/utils/pc_keyboard_shortcuts_reference.dart';

void main() {
  test('pc_keyboard_shortcuts_reference compiles', () {
    // No runtime behavior.
    expect(true, isTrue);
  });
}
