# SmartLedger 보안 시스템 상세 기록 (ROOT/USER/ASSET)

> 최종 업데이트: 2026-02-28
> 상태: **Production** (릴리즈 빌드 설치 완료)

---

## 0. 현재 적용 원칙 (확정)

- **지문/생체인식은 공통**: 모바일 OS(기기 보안) 인증을 공통 사용
- **PIN/비밀번호는 완전 분리**: `ROOT`, `USER`, `ASSET` 각각 별도 저장소/별도 정책 카운터 사용
- **보안 게이트 분리**:
  - ROOT: `RootAuthGate`
  - USER: `UserAccountAuthGate`
  - ASSET: `AssetRouteAuthGate` + 자산 탭 인증 흐름

### 0.1 서비스 분리 매핑

| 보안 영역 | PIN 서비스 | 비밀번호 서비스 | 생체인식 |
|-----------|------------|-----------------|----------|
| ROOT | `RootPinService` | `RootPinService`(ROOT password store) | `AuthService.authenticateDevice()` |
| USER | `UserPinService` | `UserPasswordService` | `AuthService.authenticateDevice()` |
| ASSET | `AssetPinService` | `AssetPasswordService` | `AuthService.authenticateDevice()` |

### 0.2 키 분리 상태

- ROOT: `root*` 키군
- USER: `user*` 키군
- ASSET: `asset*` 키군
- 세션/레거시 호환: `assetAuthSessionUntilMs`, `biometricAuthEnabled`는 호환 목적 유지

### 0.3 투자 분석 고지/동의 게이트 (법적 고지)

- 자산의 `AI 투자 참고정보` 화면은 **사전 고지/동의(체크박스 필수)** 후에만 진입 허용.
- 미동의 시 화면 진입 차단(`maybePop`) 처리.
- 동의 기록 저장 키:
  - `ai_investment_consent_accepted_v1`
  - `ai_investment_consent_accepted_at_ms_v1`
  - `ai_investment_consent_version_v1`
  - `ai_investment_consent_locale_v1`
- 운영 관리: 설정 > 법적 고지 > 투자 분석 동의 기록에서 조회/초기화 가능.

---

## 1. 아키텍처 개요

```
┌─────────────────────────────────────────────────────┐
│                  ROOT 보안 흐름                       │
├─────────────────────────────────────────────────────┤
│                                                     │
│  사용자 → RootAuthGate (인증 게이트)                  │
│            ├─ 단일 인증 → 1개 통과 시 접근 허용       │
│            └─ 2중 인증 → 2개 모두 통과 시 접근 허용   │
│                                                     │
│  인증 방식:                                          │
│    ① PIN (6자리 숫자)                                │
│    ② 생체인식 (지문/얼굴)                             │
│    ③ 비밀번호 (영문+숫자, 8자 이상)                   │
│                                                     │
│  설정 화면 → RootSecuritySetupScreen                 │
│  해싱 서비스 → RootPinService                         │
│  키 저장 → SharedPreferences (PrefKeys)              │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 2. 파일 구조

| 파일 | 줄수 | 역할 |
|------|------|------|
| `lib/services/root_pin_service.dart` | 367 | PIN/비밀번호 해싱, 검증, lockout 정책 |
| `lib/widgets/root_auth_gate.dart` | 506 | ROOT 접근 시 인증 게이트 위젯 |
| `lib/screens/root_security_setup_screen.dart` | 666 | ROOT 보안 설정 UI |
| `lib/services/user_pin_service.dart` | - | USER PIN 해싱/검증/lockout |
| `lib/services/user_password_service.dart` | - | USER 비밀번호 해싱/검증/lockout |
| `lib/widgets/user_account_auth_gate.dart` | - | USER 단일/2중 인증 게이트 |
| `lib/services/asset_pin_service.dart` | - | ASSET PIN 해싱/검증/lockout |
| `lib/services/asset_password_service.dart` | - | ASSET 비밀번호 해싱/검증/lockout |
| `lib/widgets/asset_route_auth_gate.dart` | - | ASSET 단일/2중 인증 게이트 + 세션 |
| `lib/screens/asset_security_settings_screen.dart` | - | ASSET 보안 설정 UI |
| `lib/utils/pref_keys.dart` | 266 | SharedPreferences 키 상수 |
| `lib/services/auth_service.dart` | - | 기기 생체인식 래퍼 |
| `lib/utils/dev_overrides.dart` | - | 개발용 보안 바이패스 플래그 |

---

## 3. 암호화 사양

### 3.1 해싱 알고리즘

| 항목 | 값 |
|------|-----|
| 알고리즘 | **PBKDF2-HMAC-SHA256** |
| 반복 횟수 | **150,000회** (`defaultIterations`) |
| Salt 길이 | **16바이트** (랜덤, `Random.secure()`) |
| 파생 키 길이 | **256비트** (32바이트) |
| 라이브러리 | `package:cryptography` (`Pbkdf2 + Hmac.sha256()`) |

### 3.2 보안 방어 메커니즘

| 방어 | 구현 |
|------|------|
| **Timing Attack 방어** | `_constantTimeEquals()` — XOR 기반 상수 시간 비교 |
| **Brute-force 방어** | 단계적 lockout 정책 (아래 참조) |
| **Rainbow Table 방어** | 개별 랜덤 salt (16바이트) |
| **입력 검증 (PIN)** | `RegExp(r'^\d{6}$')` — 정확한 6자리 숫자만 허용 |
| **입력 검증 (비밀번호)** | 최소 8자 이상, 빈 값 거부 |

### 3.3 Brute-force Lockout 정책

```
실패 횟수    조치
─────────────────────────────────
  1~2회      경고 없음
  3~4회      경고 표시 (warnThreshold = 3)
  5회        1분 잠금 (cooldownThreshold = 5, cooldownDuration = 1분)
  6~9회      추가 시도 가능
  10회       15분 잠금 (longLockThreshold = 10, longLockDuration = 15분)
             실패 카운터 리셋 → 다시 10회 기회
```

- PIN과 비밀번호는 **독립적인 lockout 카운터** 사용
- 성공 시 실패 카운터 + 잠금 시간 모두 즉시 리셋

---

## 4. 저장소 키 (SharedPreferences)

### 4.1 PIN 저장소

| PrefKeys 상수 | 실제 키 | 용도 |
|---------------|---------|------|
| `rootPinSaltB64` | `root_pin_salt_b64` | PIN salt (base64) |
| `rootPinHashB64` | `root_pin_hash_b64` | PIN 해시 (base64) |
| `rootPinIterations` | `root_pin_iterations` | PBKDF2 반복 횟수 |
| `rootPinFailedAttempts` | `root_pin_failed_attempts` | PIN 연속 실패 횟수 |
| `rootPinLockedUntilMs` | `root_pin_locked_until_ms` | PIN 잠금 해제 시각 (ms) |

### 4.2 비밀번호 저장소 (PIN과 완전 분리)

| PrefKeys 상수 | 실제 키 | 용도 |
|---------------|---------|------|
| `rootPasswordSaltB64` | `root_password_salt_b64` | 비밀번호 salt (base64) |
| `rootPasswordHashB64` | `root_password_hash_b64` | 비밀번호 해시 (base64) |
| `rootPasswordIterations` | `root_password_iterations` | PBKDF2 반복 횟수 |
| `rootPasswordFailedAttempts` | `root_password_failed_attempts` | 비밀번호 연속 실패 횟수 |
| `rootPasswordLockedUntilMs` | `root_password_locked_until_ms` | 비밀번호 잠금 해제 시각 (ms) |

### 4.3 보안 설정 키

| PrefKeys 상수 | 실제 키 | 값 예시 | 용도 |
|---------------|---------|---------|------|
| `rootAuthEnabled` | `root_auth_enabled` | `true/false` | ROOT 인증 활성화 여부 |
| `rootSecurityMode` | `root_security_mode` | `'pin'/'biometric'/'password'` | 기본 보안 방식 |
| `rootSecurityLevel` | `root_security_level` | `'single'/'dual'` | 보안 강도 |
| `rootPinEnabled` | `root_pin_enabled` | `true/false` | PIN 방식 활성화 |
| `rootBiometricEnabled` | `root_biometric_enabled` | `true/false` | 생체인식 활성화 |
| `rootPasswordEnabled` | `root_password_enabled` | `true/false` | 비밀번호 방식 활성화 |

---

## 5. RootPinService API

### 5.1 PIN 관련

```dart
bool isPinConfigured(SharedPreferences prefs)
Future<void> setPin(prefs, {required String pin, int iterations = 150000})
Future<bool> verifyPin(prefs, {required String pin})
Future<RootPinPolicyResult> verifyPinWithPolicy(prefs, {required String pin})
bool isLockedOut(SharedPreferences prefs)
Duration? lockRemaining(SharedPreferences prefs)
Future<void> clearLockIfExpired(SharedPreferences prefs)
Future<void> clearPin(SharedPreferences prefs)
```

### 5.2 비밀번호 관련 (별도 저장소)

```dart
bool isPasswordConfigured(SharedPreferences prefs)
Future<void> setPassword(prefs, {required String password, int iterations = 150000})
Future<bool> verifyPassword(prefs, {required String password})
Future<RootPinPolicyResult> verifyPasswordWithPolicy(prefs, {required String password})
Future<void> clearPassword(SharedPreferences prefs)
```

### 5.3 반환 타입

```dart
enum RootPinPolicyStatus { success, failed, locked }
enum RootPinLockType { cooldown, longLock }

class RootPinPolicyResult {
  final RootPinPolicyStatus status;
  final int? failedAttempts;
  final bool? showWarning;
  final RootPinLockType? lockType;
  final Duration? lockRemaining;
}
```

---

## 6. RootAuthGate 인증 흐름

### 6.1 진입점

```
_check() → SharedPreferences 로드
  ├─ kDevBypassSecurity == true → 바이패스
  ├─ !kReleaseMode + bypassSecurityForTesting → 바이패스
  ├─ rootAuthEnabled == false → 바이패스
  └─ rootAuthEnabled == true → _promptAuthentication()
```

### 6.2 단일 인증 (`_authenticateSingle`)

```
_enabledMethods() → 활성화된 방식 목록
  ├─ 0개 → 레거시 rootSecurityMode로 폴백 → _runAuth(mode)
  ├─ 1개 → 바로 _runAuth(method)
  └─ 2개+ → _showMethodSelectionDialog() → 선택 → _runAuth(selected)
성공 시 → _setAuthorized()
```

### 6.3 2중 인증 (`_authenticateDual`)

```
_enabledMethods() → 정확히 2개 필요 (아니면 에러)
  ├─ 1차: _runAuth(methods[0])
  │   ├─ 실패 → 종료
  │   └─ 성공 → "1차 인증 통과" 다이얼로그
  └─ 2차: _runAuth(methods[1])
      ├─ 실패 → 종료
      └─ 성공 → _setAuthorized() + "2중 인증 완료" 스낵바
```

### 6.4 통합 인증 실행기 (`_runAuth`)

```dart
Future<bool> _runAuth(String method, SharedPreferences prefs) {
  switch (method) {
    case 'pin':        return _authenticatePinWithResult(prefs);
    case 'biometric':  return _authenticateBiometricWithResult(prefs);
    case 'password':   return _authenticatePasswordWithResult(prefs);
  }
}
```

### 6.5 생체인식 폴백

```
_authenticateBiometricWithResult()
  ├─ 성공 → true
  ├─ AuthStatus.unavailable →
  │   ├─ _passwordEnabled → _authenticatePasswordWithResult()
  │   ├─ _pinEnabled → _authenticatePinWithResult()
  │   └─ 둘 다 미설정 → _authenticatePasswordWithResult()
  └─ 그 외 → "생체인식 실패" 에러
```

### 6.6 비밀번호 검증 레거시 호환

```
_authenticatePasswordWithResult()
  ├─ isPasswordConfigured() == true → verifyPasswordWithPolicy() (별도 저장소)
  └─ isPasswordConfigured() == false → verifyPinWithPolicy() (레거시 PIN 저장소)
```

---

## 7. RootSecuritySetupScreen UI 구조

### 7.1 화면 구성

```
┌───────────────────────────────────────┐
│ ⓘ 설정한 보안은 ROOT 전용입니다 (파란 배너) │
├───────────────────────────────────────┤
│                                       │
│ ROOT 접근 보안 강도                     │
│ ┌─────────────┬─────────────┐         │
│ │ 🔓 단일 인증 │ 🔒 2중 인증  │         │
│ └─────────────┴─────────────┘         │
│ (SegmentedButton)                     │
│                                       │
│ ROOT 접근 보호 방식 선택                 │
│ 선택된 방식: N개                        │
│                                       │
│ ┌─────────────────────────────┐       │
│ │ 📱 PIN         [사용중] ☑   │       │
│ │   6자리 숫자 PIN으로 보호     │       │
│ └─────────────────────────────┘       │
│                                       │
│ ┌─────────────────────────────┐       │
│ │ 🖐 지문/생체인식  [사용중] ☑  │       │
│ │   기기 지문 또는 얼굴인식     │       │
│ └─────────────────────────────┘       │
│                                       │
│ ┌─────────────────────────────┐       │
│ │ 🔑 비밀번호     [사용중] ☑   │       │
│ │   영문/숫자 조합 비밀번호     │       │
│ └─────────────────────────────┘       │
│                                       │
├───────────────────────────────────────┤
│        [ 저장 ]                        │
└───────────────────────────────────────┘
```

### 7.2 저장 검증 규칙

| 보안 강도 | 최소 선택 | 최대 선택 | 검증 |
|-----------|----------|----------|------|
| 단일 (`single`) | 1개 | 제한 없음 | `enabledCount >= 1` |
| 2중 (`dual`) | 2개 | 2개 | `enabledCount == 2` |

### 7.3 설정 시 동작

| 방식 | 미설정 상태에서 체크 시 | 설정 완료 상태에서 체크 시 |
|------|----------------------|------------------------|
| PIN | PIN 설정 다이얼로그 → 6자리 숫자 검증 → 저장 | 즉시 활성화 |
| 생체인식 | 기기 인증 테스트 → 성공 시 활성화 | 기기 인증 테스트 → 성공 시 활성화 |
| 비밀번호 | 비밀번호 설정 다이얼로그 → 8자+ 검증 → 저장 | 즉시 활성화 |

---

## 8. 코드 리뷰 결과 및 수정 이력

### 2026-02-28 리뷰 (6개 문제 발견 → 전체 수정 완료)

| # | 심각도 | 문제 | 원인 | 수정 내용 | 수정 파일 |
|---|--------|------|------|-----------|----------|
| 1 | **심각** | PIN과 비밀번호가 같은 해시 저장소 공유 | `setPassword()`가 `setPin()`을 호출하여 동일 키에 저장 | PIN/비밀번호 별도 해시 키 5개씩 분리 | `pref_keys.dart`, `root_pin_service.dart` |
| 2 | **심각** | 2중 인증 PIN+비밀번호 조합 불가 | 1번 문제로 인해 마지막 설정이 이전 것을 덮어씀 | 1번 수정으로 자동 해결 | — |
| 3 | **중간** | auth_gate에 ~200줄 중복 코드 | `_authenticateXxx()`와 `_authenticateXxxWithResult()` 쌍이 거의 동일 | WithResult만 유지, `_runAuth()` 헬퍼 도입, 686줄→506줄 | `root_auth_gate.dart` |
| 4 | **중간** | 2중 인증 1차 실패 시 흐름 문제 | switch문 반복, 흐름이 불명확 | `_runAuth()` 통합으로 깔끔한 순차 흐름 | `root_auth_gate.dart` |
| 5 | **경미** | 생체인식 폴백 시 비밀번호만 대체 | `AuthStatus.unavailable` 시 무조건 비밀번호 인증 | 비밀번호 우선, 미설정 시 PIN으로 폴백 | `root_auth_gate.dart` |
| 6 | **경미** | PIN 숫자 전용 검증 부재 | `pin.length != 6`만 체크, 문자 허용됨 | `RegExp(r'^\d{6}$')` 추가 | `root_security_setup_screen.dart`, `root_auth_gate.dart` |

---

## 9. 보안 바이패스 (개발 전용)

| 바이패스 | 조건 | 릴리즈에서 |
|----------|------|-----------|
| `kDevBypassSecurity` | 컴파일 타임 플래그 (`dev_overrides.dart`) | ❌ 비활성 |
| `bypassSecurityForTesting` | SharedPreferences 키 | ❌ `!kReleaseMode` 조건으로 차단 |

---

## 10. 빌드 정보

| 항목 | 값 |
|------|-----|
| APK 크기 | 107.9MB |
| 빌드 명령 | `flutter build apk --release` |
| 대상 기기 | Samsung Galaxy S901N (Android 16) |
| 기기 ID | R5CT60Q5FYR |
| flutter analyze | **No issues found** |
| 마지막 설치 | 2026-02-28 |

---

## 11. 향후 고려 사항

- [ ] SecureStorage 마이그레이션: SharedPreferences → flutter_secure_storage (해시는 이미 안전하지만 salt 보호 강화)
- [ ] PIN 변경 시 기존 PIN 확인 후 변경 기능
- [ ] 비밀번호 복잡도 강화: 영문+숫자+특수문자 조합 강제
- [ ] 인증 세션 유지: `rootAuthSessionUntilMs` 활용하여 N분간 재인증 면제
- [ ] 2중 인증 1차 실패 시 재시도 루프 (현재: 1차 실패→종료)
- [ ] 원격 잠금/초기화: Firebase 연동 가능성

---

## 12. 2026-02-28 추가 반영 사항 (자산/사용자 분리)

### 12.1 완료된 설계 반영

- [x] ASSET 전용 PrefKeys 추가 (`assetSecurityLevel`, `assetAuthEnabled`, `assetPin*`, `assetPassword*` 등)
- [x] ASSET 전용 해싱 서비스 추가 (`AssetPinService`, `AssetPasswordService`)
- [x] ASSET 보안 설정 화면을 USER 저장소에서 ASSET 저장소로 전환
- [x] ASSET 인증 게이트/자산 탭 인증을 USER 인증에서 ASSET 인증으로 전환
- [x] `flutter analyze` 통과, 릴리즈 빌드/설치 완료

### 12.2 운영 의미

- ROOT/USER/ASSET 중 한 영역의 PIN/비밀번호 변경이 다른 영역에 영향을 주지 않음
- 지문은 단일 기기 인증 레이어를 공유하여 UX 일관성 유지
- 정책(lockout, 실패 카운터, 잠금 시간)은 각 영역 저장소에서 독립 관리

---

## 13. 앱 판매 시 구독결제 적용 기준 (운영/보안)

> 적용 범위: 앱 내 디지털 기능/콘텐츠를 구독 형태로 판매하는 경우

### 13.1 결제 채널 원칙

- iOS/Android 앱 내 디지털 상품은 원칙적으로 각 스토어 인앱결제(IAP) 사용
- 외부 결제 링크/우회 결제는 스토어 정책 위반 소지가 있으므로 배포 채널별 정책 점검 필수
- 웹 결제를 병행할 경우, 앱 기능 해금 기준은 **서버 구독 상태**로 단일화

### 13.2 서버 검증 필수 항목

- 클라이언트 결제 결과만으로 권한 해금 금지
- 서버에서 영수증/구매 토큰 검증 후 구독 상태 저장
- 최소 저장 필드:
  - `userId`
  - `productId`
  - `platform` (android/ios/web)
  - `purchaseTokenOrTransactionId`
  - `originalTransactionId` (가능 시)
  - `expiresAt`
  - `status` (`active`, `grace`, `paused`, `expired`, `refunded`, `revoked`)

### 13.3 구독 상태 머신 (권한 제어 기준)

| 상태 | 앱 권한 | 운영 조치 |
|------|--------|----------|
| `active` | 유료 기능 허용 | 정상 |
| `grace` | 제한적 허용(정책 선택) | 결제수단 갱신 안내 |
| `paused` | 유료 기능 차단 또는 제한 | 재개 유도 |
| `expired` | 유료 기능 차단 | 재구독 유도 |
| `refunded` / `revoked` | 즉시 차단 | 감사 로그 기록 |

- 권한 판정 소스는 서버 응답 1순위
- 오프라인 캐시는 짧은 TTL로만 허용 (예: 수 시간)

### 13.4 앱 내 안내/고지 (분쟁 방지)

- 결제 전 화면에 가격, 결제주기, 자동갱신, 해지 방법(스토어 관리 경로) 명시
- 무료체험이 있으면 시작일/과금 시작일/해지 기한을 명확히 표시
- 환불은 각 스토어 정책에 따름을 명시

### 13.5 정산/회계 체크

- 매출 인식 기준: 갱신 시점(또는 회계 정책)으로 내부 문서화
- 스토어 수수료, VAT/부가세 반영한 순매출 집계 분리
- 환불/취소/차지백 이벤트를 월 결산에 별도 반영

### 13.6 보안 체크리스트 (릴리즈 전)

- [ ] 결제 성공 후 권한 해금이 서버 검증을 통과한 경우에만 발생
- [ ] 만료/환불/취소 이벤트 수신 시 권한이 자동 회수됨
- [ ] 계정 전환 시 구독 권한 캐시가 즉시 분리/초기화됨
- [ ] 네트워크 장애 시 권한 상승(오인증) 없이 안전 실패(fail-safe) 처리
- [ ] 구독 상태 변경 이벤트가 감사 로그에 기록됨
